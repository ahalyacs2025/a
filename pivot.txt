# Grouping
grouped = df.groupby(['Region', 'Product_Category']).agg({
    'Total_Sales': 'sum',
    'Quantity': 'sum'
}).reset_index()

print(grouped)

# Pivot Table
pivot = pd.pivot_table(
    df,
    values='Total_Sales',
    index='Region',
    columns='Product_Category',
    aggfunc='sum'
)

print(pivot)
